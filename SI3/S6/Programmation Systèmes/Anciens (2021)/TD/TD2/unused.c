// Ces fonctions ci-dessous ne servent à rien pour effectuer le tri
void foo(int i)
{
    int t = i;
    t = t + 1;
}

int bar(int i, int j, int k)
{
    int t[] = {i, j, k};
    int l = t[0];
    return i + j + k + l;
}
